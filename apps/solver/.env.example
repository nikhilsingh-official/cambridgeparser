# copy to .env.local and fill in for a non-local Supabase project.
# Both fall back to the local emulator defaults when unset, so `npm run dev`
# works with no .env present.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
